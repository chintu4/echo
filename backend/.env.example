# Copy to `.env` and fill with your values
# IMPORTANT: This file is a template. Do NOT put real credentials here or commit `.env`.
# Copy this file to `.env` and set real values in your local environment only.
# Server
PORT=5000
NODE_ENV=development

# JWT secret
SECRET_KEY=your_jwt_secret_here

# Database (replace placeholders with your DB values)
DB_HOST=your_db_host
DB_PORT=your_db_port
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
DB_INIT_RETRIES=5
# Optional: connection timeout (ms) used when attempting to connect to the DB during init
# If the DB host is unreachable, a shorter timeout fails fast (default 5000)
# DB_CONNECT_TIMEOUT_MS=5000
